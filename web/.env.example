# API base should stay relative in production behind same domain.
VITE_API_BASE=/api

# Public app base URL used for links and Stripe success/cancel redirects.
VITE_APP_BASE=https://fab.gops.app

# Stripe publishable key for browser card collection.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
